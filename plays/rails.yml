---
- name: Setup before gathering facts
  hosts: servers
  gather_facts: no

  roles:
    - setup

- name: Install nginx mysql rbenv pyenv nodenv
  hosts: servers

  roles:
    - nginx
    - puma
    - mysql
    - rbenv
    - nodenv
    - rails
